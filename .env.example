VITE_API_GATEWAY_URL=http://localhost:8000

# auth, users y soporte viajan frontend -> API Gateway -> BFF -> microservicio,
# por lo que usan VITE_API_GATEWAY_URL (arriba) y ya no necesitan variable propia.
